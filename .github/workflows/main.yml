name: Docker Image CI

on: [push]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: Install the dependencies
      run: bin/development/setup
    - name: Run test suite
      run: docker-compose run api rspec
